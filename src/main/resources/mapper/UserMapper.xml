<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mydemo.dao.UserMapper">

    <!-- type为实体类Student，包名已经配置，可以直接写类名 -->
    <resultMap id="user_map" type="com.mydemo.domain.User">
        <id property="id" column="id" jdbcType="BIGINT"/>
        <result property="userName" column="user_name" jdbcType="VARCHAR" />
        <result property="name" column="name" jdbcType="VARCHAR" />
        <result property="password" column="password" jdbcType="VARCHAR" />
        <result property="createTime" column="create_time" jdbcType="DATE"/>
        <result property="updateTime" column="update_time" jdbcType="DATE"/>
    </resultMap>

    <resultMap id="user_list_map" type="com.mydemo.domain.vo.UserVo" extends="user_map">
    </resultMap>

    <select id="getCount" resultType="Long">
        select count(id) from user where 1=1
    </select>

    <select id="getList" resultMap="user_list_map">
        select * from user where 1=1
        <if test="pager.sort!=null">
            order by m.${pager.sort} ${pager.order}
        </if>
    </select>
</mapper>